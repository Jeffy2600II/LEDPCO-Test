{
  "name": "line-bot-broadcaster",
  "version": "1.0.0",
  "description": "Simple Line Bot Broadcaster for testing",
  "main": "src/api/webhook.ts",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3"
  }
}